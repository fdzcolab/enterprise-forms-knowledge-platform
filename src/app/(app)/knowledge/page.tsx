import { KnowledgeQuery } from "@/components/knowledge-query";import { requireUser } from "@/modules/auth/session";import { userHasPermission } from "@/modules/auth/authorization";
export default async function Knowledge(){const user=await requireUser();if(!(await userHasPermission(user.id,"KNOWLEDGE_SEARCH"))&&!(await userHasPermission(user.id,"KNOWLEDGE_ANALYTICS")))throw new Error("FORBIDDEN");return <section><div className="page-head"><div><span className="eyebrow">RAG + تحلیل ساختاریافته</span><h1>دانش و تحلیل</h1><p>سامانه براساس نوع سؤال بین کوئری ساختاریافته، بازیابی معنایی یا حالت ترکیبی انتخاب می‌کند.</p></div></div><KnowledgeQuery/></section>}
