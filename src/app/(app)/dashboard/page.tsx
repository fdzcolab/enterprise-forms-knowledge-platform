import { prisma } from "@/lib/db/prisma";import { requireUser } from "@/modules/auth/session";
export default async function Dashboard(){const user=await requireUser();const [forms,myDrafts,submitted,approved,pendingReview]=await Promise.all([prisma.formDefinition.count({where:{isActive:true,currentVersion:{status:"PUBLISHED"}}}),prisma.formSubmission.count({where:{createdByUserId:user.id,status:{in:["DRAFT","IN_PROGRESS"]}}}),prisma.formSubmission.count({where:{createdByUserId:user.id,status:{in:["SUBMITTED","UNDER_REVIEW"]}}}),prisma.formSubmission.count({where:{createdByUserId:user.id,status:"APPROVED"}}),prisma.formSubmission.count({where:{status:{in:["SUBMITTED","UNDER_REVIEW"]}}})]);const cards=[["فرم‌های فعال",forms],["پیش‌نویس‌های من",myDrafts],["در انتظار بازبینی",submitted],["تأییدشده‌های من",approved],["صف بازبینی",pendingReview]];return <section><div className="page-head"><div><span className="eyebrow">نمای کلی</span><h1>داشبورد</h1><p>وضعیت فرم‌ها، ارسال‌ها و چرخه دانش سازمانی.</p></div></div><div className="stats-grid">{cards.map(([label,value])=><div className="stat-card" key={String(label)}><span>{label}</span><strong>{value}</strong></div>)}</div><div className="panel"><h2>چرخه سامانه</h2><div className="flow">ورود/ساخت فرم <b>→</b> تکمیل سنتی یا گفت‌وگویی <b>→</b> بازبینی <b>→</b> نمایه‌سازی <b>→</b> جست‌وجو و تحلیل</div></div></section>}
