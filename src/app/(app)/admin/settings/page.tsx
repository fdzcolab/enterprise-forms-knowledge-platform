import { prisma } from "@/lib/db/prisma";import { requireUser } from "@/modules/auth/session";import { requirePermission } from "@/modules/auth/authorization";
export default async function Settings(){const user=await requireUser();await requirePermission(user.id,"SYSTEM_CONFIGURE");const [sync,index]=await Promise.all([prisma.externalKnowledgeSync.groupBy({by:["status"],_count:{_all:true}}),prisma.knowledgeIndexRecord.groupBy({by:["status"],_count:{_all:true}})]);return <section><div className="page-head"><div><span className="eyebrow">پیکربندی و سلامت</span><h1>تنظیمات سامانه</h1><p>کلیدهای محرمانه فقط از محیط سرور خوانده می‌شوند و در مرورگر نمایش داده نمی‌شوند.</p></div></div><div className="settings-grid"><div className="panel"><h2>مدل‌ها و ذخیره‌سازی</h2><dl><dt>LLM</dt><dd>{process.env.LLM_MODEL||"تنظیم نشده"}</dd><dt>Embedding</dt><dd>{process.env.EMBEDDING_MODEL||"تنظیم نشده"}</dd><dt>Qdrant</dt><dd>{process.env.QDRANT_URL||"تنظیم نشده"}</dd></dl></div><div className="panel"><h2>OpenWebUI</h2><dl><dt>همگام‌سازی</dt><dd>{process.env.OPENWEBUI_SYNC_ENABLED==="true"?"فعال":"غیرفعال"}</dd><dt>نشانی</dt><dd>{process.env.OPENWEBUI_BASE_URL||"—"}</dd><dt>Knowledge Base</dt><dd>{process.env.OPENWEBUI_KNOWLEDGE_BASE_ID||"—"}</dd><dt>API Key</dt><dd>{process.env.OPENWEBUI_API_KEY?"ذخیره شده (مخفی)":"تنظیم نشده"}</dd></dl><form action="/api/admin/openwebui/test" method="post"><button className="btn secondary">آزمایش اتصال</button></form></div><div className="panel"><h2>وضعیت نمایه‌سازی داخلی</h2>{index.map(x=><div className="metric-row" key={x.status}><span>{x.status}</span><strong>{x._count._all}</strong></div>)}</div><div className="panel"><h2>وضعیت OpenWebUI Sync</h2>{sync.map(x=><div className="metric-row" key={x.status}><span>{x.status}</span><strong>{x._count._all}</strong></div>)}{!sync.length&&<div className="empty">رکوردی وجود ندارد.</div>}</div></div></section>}
