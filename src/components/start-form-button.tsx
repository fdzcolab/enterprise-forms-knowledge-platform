"use client";import { useState } from "react";export function StartFormButton({formDefinitionId}:{formDefinitionId:string}){const[busy,setBusy]=useState(false);return <button className="btn primary" disabled={busy} onClick={async()=>{setBusy(true);const r=await fetch("/api/web/submissions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({formDefinitionId})});const j=await r.json();if(r.ok)location.href=`/submissions/${j.data.id}`;else{alert(j.error?.message||"خطا");setBusy(false);}}}>{busy?"در حال ایجاد…":"شروع فرم"}</button>}
