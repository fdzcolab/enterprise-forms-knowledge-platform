"use client";
import { useState } from "react";import { authClient } from "@/lib/auth-client";
export function LoginForm(){const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);return <form className="auth-card" onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");const r=await authClient.signIn.username({username,password});setBusy(false);if(r.error)setError("نام کاربری یا رمز عبور نادرست است.");else location.href="/dashboard";}}><div className="brand-mark">EFK</div><h1>فرم‌یار سازمانی</h1><p>ورود به سامانه فرم‌ها و دانش سازمانی</p><label>نام کاربری<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required/></label><label>رمز عبور<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label>{error&&<div className="alert error">{error}</div>}<button className="btn primary" disabled={busy}>{busy?"در حال ورود…":"ورود"}</button></form>}
